#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back
#define all(x) (x).begin(), (x).end()
#define FAST_IO ios::sync_with_stdio(false); cin.tie(nullptr);

int main() {
    FAST_IO;

int t=1;
// cin>>t;
while(t--)
{
    string s;
    cin>>s;
    vector<int> n(3),p(3);
    for(int i=0;i<3;i++)
    {
        cin>>n[i];
    }
    for(int i=0;i<3;i++)
    {
        cin>>p[i];
    }
    int r;
    cin>>r;
    //unsolved
}

   return 0;
}
